# 基本設計書テンプレート

> **位置づけ**: `/speckit.design basic` を実行すると AI が本テンプレートをもとに基本設計書を生成します。
> 生成後、人間がレビューし内容を承認してください。
> 本ファイルを直接編集する必要はありません。
>
> 関連: [憲法 §7](../.specify/memory/constitution.md) / [利用指南書 §10](how-to-use.md)

---

<!-- AI生成時の指示: 本テンプレートに従い、HTMLベースでUMLに準拠した基本設計書を生成すること -->

## メタ情報

| 項目 | 内容 |
|---|---|
| ドキュメントID | BASIC-[連番] |
| 対象フィーチャー | （`specs/[###-feature-name]/` へのリンク） |
| 作成日 | |
| 作成者 | AI生成（`/speckit.design basic`） |
| 承認者 | |
| 承認日 | |
| バージョン | 1.0 |

---

## 1. システム概要

### 1.1 目的

（`spec.md` の Overview セクションから引用する）

### 1.2 対象範囲

（`spec.md` のスコープから引用する）

---

## 2. アーキテクチャ概要

### 2.1 システム構成図（UML: コンポーネント図）

> 生成形式: HTML + SVG（インラインSVG または `<img>` タグ）。
> PlantUML 記法でのソースも `<!-- plantuml: ... -->` としてコメントに残すこと。

```html
<!-- ここにHTMLベースのコンポーネント図を生成する -->
<!-- 例: フロントエンド / バックエンド / DB の構成を矩形と矢印で表現 -->
```

### 2.2 採用アーキテクチャパターン

（`tech-stack.md` の「全体構成」から引用する）

---

## 3. 機能一覧

| 機能ID | 機能名 | 概要 | 対応要件ID |
|---|---|---|---|
| F-001 | | | REQ-xxx |

---

## 4. 画面設計

### 4.1 画面一覧

| 画面ID | 画面名 | URL / ルート | 概要 |
|---|---|---|---|
| SCR-001 | | | |

### 4.2 画面遷移図（UML: ステートマシン図）

```html
<!-- ここにHTMLベースの画面遷移図を生成する -->
```

---

## 5. API 設計概要

> 詳細は `specs/[###]/contracts/` を参照。ここでは主要エンドポイントの一覧のみ示す。

| メソッド | パス | 概要 | 対応機能ID |
|---|---|---|---|
| GET | /api/... | | F-xxx |
| POST | /api/... | | F-xxx |

---

## 6. データ概念モデル（UML: クラス図 / ER図）

> 詳細は `specs/[###]/data-model.md` を参照。ここではエンティティ間の関係のみ示す。

```html
<!-- ここにHTMLベースのER図またはクラス図を生成する -->
```

---

## 7. 非機能設計方針

| 区分 | 方針 | 根拠ドキュメント |
|---|---|---|
| 性能 | | `requirements.md` §4 |
| セキュリティ | | `constitution.md` §1, `requirements.md` §4 |
| 可用性 | | `requirements.md` §4 |
| 保守性 | | `constitution.md` §1 |

---

## 8. 承認

| 役割 | 氏名 | 承認日 | 判定（承認／差し戻し） |
|---|---|---|---|
| 作成者（AI生成確認者） | | | — |
| アーキテクト / テックリード | | | |
| PM / プロジェクトリーダー | | | |
