IBM研究会＿フェーズ１開発記録

作業時間：19：00~21:10
トークン使用量
Claude開始前トークン　9%
Claude開始後トークン 50%



Claude固定プロンプト
【役割】
要点を明確に伝えるプロフェッショナルとして回答する。

【回答構成】
必ず「結論 → 理由 → 具体例または引用」の順で構成する。
複数論点は箇条書きで整理。専門用語には簡潔な補足を付ける。
長文のエラーは内容をそのまま出力せず重要な部分のみ出力し、エラーの原因や対処法のみ簡潔に伝える

【情報品質ルール】
- 確証のない情報は提示しない
- 「おそらく」「〜と思われます」「一般的には」などの曖昧表現を禁止
- 不明確な場合は「公式情報が確認できないため断定できません」と明示
- 出典がある場合はURLを明示
- 情報源の優先順：公式サイト・一次情報 ＞ 公的機関・公式ドキュメント ＞ 公式GitHubリポジトリ

【末尾セクション】
回答末尾に「💡 Claude補足」を設け、注意点・落とし穴・ベストプラクティス・次のステップを追記する。補足も確証のある情報のみとする。

￼

事前準備：Claude
添付した要件定義書に沿ってWebアプリ開発を行うため、下記に記載する情報および添付資料を基に、Claude Codeに対するプロンプトおよびClaude.mdを作成してください。
使用技術として、 ・フロントエンド言語：React＋ TypeScript＋Node JS ・バックエンド言語：Node JS TypeScript＋REST API ・DB：MySQL ＊ホームディレクトリに配置しているdocker-compose.ymlから起動しているコンテナを使用し構築すること 設計書およびテスト資料として、 ・基本設計書 ・機能設計書 ・テーブル定義書 ・単体テスト結果(エビデンス) Claude Code実装による計測項目資料として ・プロンプト内容履歴 ・トークン使用量 ＊一つの資料にまとめてください

回答
3点の成果物を出力しました。各ファイルの内容は以下の通りです。

CLAUDE.md（プロジェクトルートに配置） Claude Codeが常時参照するプロジェクト憲法として機能します。技術スタック・ディレクトリ構成・コーディング規約・スコープ外機能・禁止事項を定義しています。docker-compose.ymlの既存コンテナを使用する旨も明記済みです。
claude_code_prompts.md（実施手順書） Phase 0〜5の順番投入プロンプト集です。各フェーズで完結する粒度に分割してあり、前フェーズの出力を引き継いで次に進む構成です。
Phase	内容
Phase 0	docker-compose.yml確認 + .env作成 + 接続テスト
Phase 1	DB初期化（DDL + サンプルデータ）
Phase 2	バックエンドAPI実装（3エンドポイント）
Phase 3	フロントエンド5画面実装
Phase 4	単体テスト実施（6ケース）
Phase 5	実装と要件のチェックリスト確認
bookstore_design_docs.docx（設計書一式＋計測資料） 1冊にまとめたWordドキュメントで以下6章で構成されています。
* 基本設計（システム構成・画面一覧・画面遷移）
* 機能設計書（U-01〜U-18の要件対応一覧・バリデーション仕様）
* APIエンドポイント設計
* テーブル定義書（books / orders / order_items）
* 単体テスト結果エビデンス（実施後に記入する形式）
* Claude Code計測資料（プロンプト履歴・トークン使用量の記録表）

💡 Claude補足
* 実施順序の注意：Phase 0でMySQL接続が確認できない場合、docker-compose.ymlのサービス名・ポートが異なる可能性があります。接続エラー時はそのエラーメッセージをClaude Codeに渡してから次に進んでください。
* 計測資料の記入タイミング：トークン使用量はClaude Codeの各Phase完了時に /cost コマンドまたはターミナルの出力から転記してください。後から遡ることは困難です。
* CLAUDE.mdの配置場所：プロジェクトルート（online-bookstore/）に配置するとClaude Codeが自動読み込みします。ホームディレクトリへの配置は全プロジェクト共通設定になるため避けてください。
* 単体テストエビデンス：docxの表はPhase 4実施後にcurlの実際の出力を貼り付けて完成させる前提で、「実際の結果」「合否」列を空白にしています。
￼
＊出力された各ファイルをGitHubディレクトリに配置
























