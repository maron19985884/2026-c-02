# backend 規約

backend 配下のファイルを扱う際に自動で読み込まれる規約。
ルートの `CLAUDE.md`（決定事項）が正（マスター）であり、本ファイルはそのサブセットの再掲。矛盾を見つけた場合はルートを優先し、本ファイルを修正すること。

## API 設計

- レスポンスは常に `{ data, error, message }` の統一フォーマット
  - 正常時: `{ data: <結果>, error: null, message: "OK" }`
  - 異常時: `{ data: null, error: "<エラーコード>", message: "<説明>" }`
- リクエストバリデーションは zod を使用する
- ポートは **4000** 固定（変更禁止）

## 実装規約

- TypeScript strict モード。`any` 型の使用禁止
- SQL はプリペアドステートメント必須（SQL インジェクション対策）
- エラーは握りつぶさない（catch して無視しない。必ず上記 API フォーマットで返す）

## 参照

- 注文番号フォーマット・`orders` テーブルのカラム命名はルート `CLAUDE.md` の「DB スキーマ規約」を参照
