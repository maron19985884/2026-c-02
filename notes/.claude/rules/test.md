# テストフェーズのルール

- `docs/specs/spec.md` の正常系・異常系フローを網羅すること
- バリデーション（REQ-013）は境界値テストを必ず含める
- カートの数量変更・合計計算（REQ-008〜010）は単体テストで確認する
- API エンドポイント（`/api/books`、`/api/orders`）は結合テストを作成する
- テストファイルは対象ファイルと同階層の `__tests__/` または `.test.ts` 形式で配置する
