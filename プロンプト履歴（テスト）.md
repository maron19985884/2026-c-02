# プロンプト履歴（テスト）

作成日: 2026-05-13  
ブランチ: feature/satoyuya

---

## セッション情報

| 項目 | 値 |
|------|-----|
| 使用モデル | claude-sonnet-4-6 |
| プラン | Pro |
| 実施日 | 2026-05-13 |
| セッション使用量 | 24% 使用済み |
| リセットまでの残り時間 | 4時間26分後にリセット |

---

## トークン使用量（推計）

### 推計根拠：処理ファイル一覧

#### 読み込みファイル（入力）

| ファイル | 行数 | 推定トークン |
|---------|------|------------|
| `backend/src/routes/orders.ts` | 139行 | 約 900 |
| `backend/src/routes/books.ts` | 45行 | 約 300 |
| `frontend/src/context/CartContext.tsx` | 127行 | 約 850 |
| `backend/src/types.ts` | 44行 | 約 250 |
| `frontend/src/types/index.ts` | 49行 | 約 280 |
| `frontend/src/lib/api.ts` | 39行 | 約 230 |
| `backend/package.json` | 22行 | 約 100 |
| `frontend/package.json` | 22行 | 約 100 |
| `frontend/src/app/checkout/page.tsx` | 225行 | 約 1,500 |
| `mysql/init/01_init.sql` | 108行 | 約 900 |
| `backend/src/index.ts` | 24行 | 約 130 |
| `backend/src/db.ts` | 14行 | 約 80 |
| `docker-compose.yml` | 90行 | 約 600 |
| `プロンプト履歴（製造）.md` | 171行 | 約 4,300 |
| `プロンプト履歴（テスト）.md` | 157行 | 約 4,000 |
| **合計** | **1,276行** | **約 14,520** |

#### 生成ファイル（出力）

| ファイル | 行数 | 推定トークン |
|---------|------|------------|
| `ユニットテスト/backend/generateOrderNumber.test.ts` | 57行 | 約 400 |
| `ユニットテスト/backend/validation.test.ts` | 117行 | 約 800 |
| `ユニットテスト/frontend/cartReducer.test.ts` | 257行 | 約 1,700 |
| `ユニットテスト/frontend/checkoutValidation.test.ts` | 80行 | 約 600 |
| `結合テスト/api/books.test.ts` | 55行 | 約 400 |
| `結合テスト/api/orders.test.ts` | 130行 | 約 950 |
| 設定ファイル 6件（package.json × 2、tsconfig.json × 2、jest.config.js × 2） | 70行 | 約 450 |
| `ユニットテスト/テスト指示書.md` | 170行 | 約 5,600 |
| `ユニットテスト/テスト結果.md` | 150行 | 約 4,800 |
| `結合テスト/テスト指示書.md` | 170行 | 約 5,600 |
| `結合テスト/テスト結果.md` | 150行 | 約 4,800 |
| `プロンプト履歴（テスト）.md` | 200行 | 約 5,000 |
| **合計** | **1,606行** | **約 31,100** |

### 推計合計

| 区分 | トークン数（推計） | 備考 |
|------|----------------|------|
| 入力：ファイル読み込み | 約 14,500 | ソースコード15ファイル |
| 入力：会話・コマンド出力 | 約 55,000 | Docker実行結果・会話履歴の蓄積 |
| **入力合計** | **約 70,000** | |
| 出力：ファイル生成 | 約 31,000 | テスト・MD・設定ファイル18件 |
| 出力：会話テキスト | 約 19,000 | 説明・回答・エラー調査 |
| **出力合計** | **約 50,000** | |
| **総合計** | **約 120,000** | |

> ※ トークン換算の目安：コード 4文字≒1トークン、日本語 1.5文字≒1トークン  
> ※ 会話履歴は各ターンに前回までの全コンテキストが含まれるため増加しやすい

---

## プロンプト 1

### 依頼内容

```
ユニットテスト、結合テストを実施してください。テストに使用する各種ファイルは、
「テスト」フォルダ内にある「ユニットテスト」フォルダ、「結合テスト」フォルダを作成して、
そこに格納してください。テスト指示書、テスト結果をmdで作成してください。
```

### 実施内容

テストフォルダ構成を作成し、ユニットテスト51件・結合テスト16件（計67件）を実施した。
全テストが PASS したことを確認し、テスト指示書・テスト結果をそれぞれ md ファイルで記録した。

#### 1. フォルダ・ファイル構成

```
テスト/
├── ユニットテスト/
│   ├── テスト指示書.md
│   ├── テスト結果.md
│   ├── package.json
│   ├── tsconfig.json
│   ├── jest.config.js
│   ├── backend/
│   │   ├── generateOrderNumber.test.ts   # 注文番号生成ロジック（4件）
│   │   └── validation.test.ts            # 注文バリデーション（20件）
│   └── frontend/
│       ├── cartReducer.test.ts           # カートReducer（17件）
│       └── checkoutValidation.test.ts    # チェックアウトバリデーション（10件）
└── 結合テスト/
    ├── テスト指示書.md
    ├── テスト結果.md
    ├── package.json
    ├── tsconfig.json
    ├── jest.config.js
    └── api/
        ├── books.test.ts                 # Books API（5件）
        └── orders.test.ts                # Orders API（11件）
```

#### 2. ユニットテスト（51件）

**テストフレームワーク:** Jest 29 + ts-jest  
**実行環境:** node:20-alpine（Docker コンテナ）  
**外部依存:** なし（純粋な関数ロジックのみテスト）

| テストファイル | 対象ソース | テスト件数 |
|-------------|---------|---------|
| `backend/generateOrderNumber.test.ts` | `backend/src/routes/orders.ts` | 4件 |
| `backend/validation.test.ts` | `backend/src/routes/orders.ts` | 20件 |
| `frontend/cartReducer.test.ts` | `frontend/src/context/CartContext.tsx` | 17件 |
| `frontend/checkoutValidation.test.ts` | `frontend/src/app/checkout/page.tsx` | 10件 |

**テスト項目概要:**

- 注文番号生成: フォーマット検証（`ORD-YYYYMMDDHHmmss-XXXX`）、範囲検証、一意性検証
- バリデーション: 氏名・メールアドレス・住所・注文商品の必須/形式チェック
- カートReducer: ADD / UPDATE / REMOVE / CLEAR / HYDRATE の各アクション、イミュータブル性、合計金額・数量計算
- チェックアウトフォーム: 各フィールドの必須/形式バリデーション、複合エラー

**テスト結果:**

```
Test Suites: 4 passed, 4 total
Tests:       51 passed, 51 total
Time:        10.656 s
```

#### 3. 結合テスト（16件）

**テストフレームワーク:** Jest 29 + ts-jest + Axios  
**実行環境:** node:20-alpine（Docker コンテナ）、`2026-c-02_app-network` に接続  
**接続先:** `http://backend:4000`（Docker 内部）

| テストファイル | 対象 API | テスト件数 |
|-------------|---------|---------|
| `api/books.test.ts` | GET /api/books、GET /api/books/:id | 5件 |
| `api/orders.test.ts` | POST /api/orders、GET /api/orders/:id | 11件 |

**テスト項目概要:**

- `GET /api/books`: 200レスポンス・レスポンス構造検証
- `GET /api/books/:id`: 正常取得・404・400 の各ケース
- `POST /api/orders`: 注文作成成功（201）・各バリデーションエラー（400）・不正書籍ID（500）
- `GET /api/orders/:id`: 注文詳細取得・注文明細件数・合計金額の正確性・404・400

**テスト結果:**

```
Test Suites: 2 passed, 2 total
Tests:       16 passed, 16 total
Time:        11.628 s
```

#### 4. テスト実行方法

**ユニットテスト（Docker Desktop 起動後）:**
```bash
docker run --rm \
  -v "d:/IBMStudy/2026-c-02/テスト/ユニットテスト:/tests" \
  -w /tests node:20-alpine \
  sh -c "npm install && npm test"
```

**結合テスト（`docker-compose up -d` 後）:**
```bash
docker run --rm \
  -v "d:/IBMStudy/2026-c-02/テスト/結合テスト:/tests" \
  -w /tests \
  --network 2026-c-02_app-network \
  -e API_URL=http://backend:4000 \
  node:20-alpine \
  sh -c "npm install && npm test"
```

#### 5. テスト結果サマリー

| テスト種別 | テスト件数 | 結果 |
|---------|---------|------|
| ユニットテスト | 51件 | 全件 PASS |
| 結合テスト | 16件 | 全件 PASS |
| **合計** | **67件** | **全件 PASS** |

---

## プロンプト 2

### 依頼内容

```
今までのプロンプトをまとめてファイルでください。ファイル名は「プロンプト履歴2.md」としてください。
```

### 実施内容

本ファイル（`プロンプト履歴2.md`）を作成。

---

## 参考：テスト対象ソースファイル

| ソースファイル | 説明 |
|-------------|------|
| `backend/src/routes/orders.ts` | 注文番号生成・バリデーション・注文作成トランザクション |
| `backend/src/routes/books.ts` | 書籍一覧・詳細取得 |
| `frontend/src/context/CartContext.tsx` | カート状態管理（useReducer） |
| `frontend/src/app/checkout/page.tsx` | 注文フォームバリデーション |
