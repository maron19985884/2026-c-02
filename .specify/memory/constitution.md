# プロジェクト憲法（Constitution）

> Spec Kit公式の位置づけ: `/speckit.constitution` により生成・更新される、
> コード品質・テスト基準・UX一貫性・パフォーマンス要件・技術的意思決定ルールなど
> チームの長期的な原則を記録するファイル。単一機能の要件ではなく、
> チーム全体の合意事項を書く場所として公式に位置づけられています。
> 出典: https://knightli.com/en/2026/05/25/github-spec-kit-spec-driven-development/
>
> **運用方針**: 憲法は **組織で一度だけ確定させ、以後は各プロジェクトへそのまま配置して再利用する**。
> プロジェクト個別の微調整が必要な場合のみ `.specify/templates/overrides/` またはpresetで上書きする。

## 1. コード品質原則（Code Quality）
- すべてのコードは、静的解析（Lint）で **エラー0件** を必須とする。警告(Warning)の扱いはプロジェクト単位でpresetに定義する。
- Lintツールは言語・スタックにより異なる（例: JavaScript/TypeScript → ESLint、Python → Ruff、Java → Checkstyle）。具体的なルールセットは本ファイルに直接書かず、`docs/lint-preset-guide.md` の手順でpreset化し、`.specify/templates/overrides/` または独自presetパッケージとして管理する。
- Lint基準の変更は本憲法の改訂手続き（レビュー＋承認）を経て反映する。

## 2. テスト基準（Testing Standards）
- 主要なビジネスロジックには単体テストを必須とする。カバレッジ目標はプロジェクトの技術選定書で定義する（未定義の場合は 80% を暫定適用する）。
- 結合テスト・E2Eテストの要否は機能の重要度に応じて判断する。重要度の基準は要件定義書の優先度（高/中/低）に従う。
- テストのない実装はレビューで差し戻す。

## 3. UX一貫性（UX Consistency）
- UIはプロジェクトの技術選定書で定義したCSSフレームワーク・デザインシステムに準拠する。未定義の場合はブラウザデフォルトスタイルを基準とし、プロジェクト内で統一する。
- 画面遷移・エラー表示・空状態（データ0件時）の扱いを全画面で統一する。
- アクセシビリティは WCAG 2.1 AA を目標とする。

## 4. パフォーマンス要件（Performance）
- 主要画面・APIのレスポンスタイムはプロジェクトの要件定義書で定義する。未定義の場合は「95パーセンタイルで2秒以内」を暫定基準とする。
- 想定同時接続数・スケーラビリティ方針は要件定義書の非機能要件に従う。未定義の場合はローカル環境での単一接続を前提とする。

## 5. 技術的意思決定ルール（Technical Decision Rules）
- 使用技術・アーキテクチャは `/speckit.plan` の前に **技術選定書（`docs/tech-stack-template.md`）** で人間が確定させる。`plan` フェーズで技術を選び直さない。
- 新規依存ライブラリを技術選定書に追加する際は、選定理由を明記する。

## 6. 開発方法論の運用ルール（Waterfall / Brownfield）
- 本プロジェクトを **ウォーターフォールで運用する場合**: 各フェーズ（要件定義／設計／実装／テスト／リリース）ごとに承認ゲートを設け、`docs/waterfall-preset-guide.md` の対応表に従って `/speckit.specify` → `/speckit.plan` → `/speckit.tasks` → `/speckit.implement` の各出力を正式ドキュメントとして承認フローに乗せる。
- 本プロジェクトが **既存システムの改修（Brownfield）である場合**: システム全体をspec化せず、`docs/brownfield-guide.md` に従い、改修範囲のみを対象とした狭いspec（`docs/change-spec-template.md`参照）を作成する。既存の設計書・規約は本憲法に転記せず、参照リンクのみを記載する。

<!--
  [変更 2026-08-16] セクション7を全面改訂。
  変更前（3行のみ）:
    - 基本設計書について、HTMLベースでUMLに準拠して作成する。
    - 詳細設計書について、HTMLベースでパラメータ・修正対象ファイル・処理概要・In/Out詳細をまとめて作成する。
    - テーブル定義書について、HTMLベースで作成する。
  変更理由:
    002-book-catalog-detail の設計書作成中、上記の抽象的な記述だけでは何を書けばよいか曖昧だったため、
    後から画面レイアウト・項目定義／外部インターフェース仕様（API一覧）／エラー処理方針／処理フロー図／
    例外処理／呼び出し関係図の6要素が抜けていることが判明し、都度追記する手戻りが発生した。
    再発防止のため、必須セクションを明示するチェックリスト形式に変更し、
    実例（docs/design-doc-guide.md）とコピー用テンプレート（docs/design-templates/）を新設した。
    データ量見積もりは検討したが、学習・デモ用途で不要と判断し必須項目には含めていない。
-->
## 7. 設計ルール
- 基本設計書・詳細設計書・テーブル定義書は、HTMLベースで作成する。`docs/design-templates/` のテンプレートをコピーして作成し、必須セクションの背景・書き方は `docs/design-doc-guide.md` に従う。
- 基本設計書は、以下のセクションを含めること: システム概要／アーキテクチャ構成図（UML準拠）／外部インターフェース仕様（API一覧）／画面一覧／画面レイアウト・項目定義／画面遷移図／シーケンス図（UML準拠）／エラー処理方針（共通）／非機能要件との対応。
- 詳細設計書は、文書冒頭に呼び出し関係図（コールツリー）を含め、モジュールごとに以下を含めること: 処理概要／処理フロー図（分岐がある場合）／例外処理／パラメータ・修正対象ファイル・In/Out詳細。
- テーブル定義書は、以下を含めること: テーブル一覧／カラム定義／インデックス定義／制約／ER図。

---
